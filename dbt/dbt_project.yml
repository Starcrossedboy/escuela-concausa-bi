name: 'faro'
version: '1.0.0'
config-version: 2

profile: 'faro'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  faro:
    silver:
      +materialized: table
