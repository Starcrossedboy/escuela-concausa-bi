# Base COMÚN del proyecto FARO. Cada célula instala ADEMÁS su stack (ver §4.2.6 de tu plan de sprint):
#   C1: apache-airflow dbt-core · C2: apache-superset · C4: fastapi python-jose passlib
#   C3: scikit-learn xgboost mlflow shap chromadb sentence-transformers (macOS: brew install libomp)
#   C5: docker-compose google-cloud-storage
# No asumas que con `pip install -r requirements.txt` tu ambiente ya quedó completo.
pandas>=2.2
polars>=1.0
pyarrow>=16.0
requests>=2.32
python-dotenv>=1.0
pydantic>=2.7
pydantic-settings>=2.3
great-expectations>=0.18
sqlalchemy>=2.0
psycopg2-binary>=2.9
ruff>=0.5
pytest>=8.0
httpx>=0.27

# Célula 4 & 5 - API y Cloud
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-jose[cryptography]>=3.3.0   # JWT (US-402). Uso endurecido: algoritmos explícitos en decode.

# Célula 3 - ML. Sólo lo que el CI necesita para correr las pruebas de src/modelos/:
# el resto del stack (xgboost, mlflow, shap, chromadb, sentence-transformers) vive en
# requirements/celula-3.txt, que el CI no instala. `mlflow` es import diferido a propósito.
scikit-learn>=1.5
